# Transport implementations for ENLP: real hardware (serial, tcp) and the
# first-class simulator (sim/). All speak the same Transport interface
# (base.py) so the rest of the gateway cannot tell them apart.
