# name,     type, subtype,  offset,   size,   flags
# Two OTA slots (S5 will need them; repartitioning a deployed fleet is not an
# option, so the layout is right from the first flash) + NVS for identity and
# the experiment spec + a LittleFS store for buffered telemetry.
nvs,        data, nvs,      0x9000,   0x6000,
otadata,    data, ota,      0xf000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x180000,
ota_1,      app,  ota_1,    0x1A0000, 0x180000,
store,      data, spiffs,   0x320000, 0xC0000,
